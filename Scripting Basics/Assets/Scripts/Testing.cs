using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour
{
    //Public Variables
    public string firstName = "Guillermo";
    public int gradeLevel = 12;
    public float GPA = 3.66f;
    public bool usingNotes = true;

    //Private Variables
    private int marioBros = 1;
    private string favMovie = "The Super Mario Bros. Movie";
    private float gasPrice = 6.09f;
    private bool wasHelpful = true;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
